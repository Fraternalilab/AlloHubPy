# AlloHubPy
Python version for the AllohubMat analysis

To install first one needs to compile the C code with python mi_bloc_extension_build.py

This codes requires the following packages:

*  numpy
* seaborn
* cffi
